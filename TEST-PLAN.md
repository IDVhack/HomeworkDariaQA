# Тест-план — 12 проверок по нововведениям сессии 3

> Составлен и утверждён пользователем в сессии 4 (см. `sessions/session-4.md`), до
> прогона. Покрывает новую функциональность сессии 3 (поиск ингредиента через
> комбобокс, поле «Вес 1 шт, г», переключатель единиц «г/шт»). Не более трети пунктов
> «позитивные» — остальные намеренно негативные/граничные: пустые поля, пробелы по
> краям, слишком длинное значение, повторная отправка того же, кириллица, пустой
> запрос, значения на границе и на единицу за границей.
>
> Результаты прогона (пройдено/не пройдено, доказательства, дефекты) — в
> `sessions/session-4.md` и `BUGS.md`. Этот файл — только план, каким он был утверждён,
> без результатов.

**1. Поиск: подстрока находит ингредиент (позитив)**
Вход: `греч`. Шаг: кликнуть в поле «Ингредиент», ввести текст. Ожидаемо: список
фильтруется по подстроке без учёта регистра.

**2. Поиск: пустой запрос (граница)**
Вход: пустая строка. Шаг: кликнуть в поле поиска, ничего не вводя. Ожидаемо: показывается
полный список ингредиентов, а не «Ничего не найдено» и не пустой список.

**3. Поиск: запрос без совпадений (негатив)**
Вход: `zzzzz`. Шаг: ввести в поле поиска. Ожидаемо: выпадающий список показывает «Ничего
не найдено», выбор ингредиента не установлен.

**4. Текст введён, но пункт не выбран кликом (негатив, подводный камень)**
Вход: вручную набрать точное имя существующего ингредиента побуквенно, не кликая по
пункту дропдауна. Шаг: нажать «Добавить». Ожидаемо: по коду выбор (`calcSelectedId`)
остаётся не установлен → просто фокус на поле, без добавления и без видимого сообщения
об ошибке — фиксируем как потенциально непонятный пользователю тихий отказ.

**5. Пробелы по краям в поисковом запросе (граница)**
Вход: `"  греч  "` — с пробелами по краям. Шаг: ввести в поле поиска. Ожидаемо: фильтрация
игнорирует пробелы по краям, результат такой же, как без пробелов.

**6. Сброс выбора после ручного редактирования текста (граница, подводный камень)**
Вход: выбрать ингредиент кликом по пункту, затем удалить один символ в поле. Шаг: клик по
пункту → Backspace. Ожидаемо: выбор сбрасывается, опция «шт» снова недоступна, «Добавить»
не сработает без повторного явного выбора из списка.

**7. Вес 1 шт: граница «задано / не задано» — 0 против 0,1 (граница, на единицу за границей)**
Вход: два ингредиента с «Вес 1 шт, г» = `0` (или пусто) и `0.1`. Шаг: сохранить оба,
выбрать каждый в калькуляторе. Ожидаемо: при 0/пусто опция «шт» задизейблена («не
задано»), при 0,1 — активна и показывает вес.

**8. Отрицательное значение веса 1 шт (негатив, граница)**
Вход: `-50`. Шаг: ввести в поле «Вес 1 шт, г» при создании ингредиента, сохранить.
Ожидаемо: по коду отрицательное значение приводится к 0 (`Math.max(0, …)`) — проверить,
что в таблице отображается «—»/0, а не -50.

**9. Повторное добавление одного и того же ингредиента (граница, подводный камень —
«повторная отправка того же»)**
Вход: «Гречка (сух.)», 100 г, дважды подряд. Шаг: выбрать → «Добавить» → снова выбрать тот
же ингредиент → «Добавить» ещё раз без изменения количества. Ожидаемо: по коду граммы
суммируются в существующей строке (100+100=200 г), новая строка не создаётся.

**10. Слишком длинный запрос в поле поиска (негатив, граничное значение по длине)**
Вход: строка ~500 символов кириллицы без пробелов. Шаг: вставить в поле поиска. Ожидаемо:
не описано явно в SPEC — проверяем, не ломает ли верстку выпадающего списка/поля
(возможный повтор известного дефекта из `BUGS.md`, сессия 2, про переполнение таблицы
длинным названием).

**11. Количество в калькуляторе: 0 против 1 (граница, на единицу за границей)**
Вход: `qty = 0`, затем `qty = 1`. Шаг: выбрать ингредиент, ввести 0 в «Количество»,
нажать «Добавить»; повторить с 1. Ожидаемо: при 0 — фокус на поле без добавления; при 1 —
успешно добавляется.

**12. Поиск с разным регистром кириллицы (граница, кириллица)**
Вход: `ГрЕчКА`. Шаг: ввести в поле поиска. Ожидаемо: фильтрация регистронезависима, результат
совпадает с вводом в нижнем регистре.
